{% macro setBg(imageset, color = null) -%}
	style="
		{%- for key, value in imageset -%}
			--bg{{ key|capitalizeFirst }}: url('../{{ value }}');
		{%- endfor -%}
		{%- if color -%}
			--bgColor: {{ color }};
		{%- endif -%}
	"
{%- endmacro %}
